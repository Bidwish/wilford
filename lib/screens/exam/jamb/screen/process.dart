import 'package:flutter/material.dart';

class JambProcessScreen extends StatelessWidget {
  const JambProcessScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
